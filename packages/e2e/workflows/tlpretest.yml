on: workflow_dispatch
jobs:
  tlpretest:
    runs-on: ubuntu-latest
    steps:
      - name: Setup TeX Live
        uses: $/.
        with:
          repository: https://ftp.math.utah.edu/pub/tlpretest/
          version: 2027
      - run: tlmgr version
